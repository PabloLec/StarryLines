{
  "private": true,
  "name": "mongo-proxy",
  "scripts": {
    "build": "worktop build index.ts",
    "types": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "realm-web": "^1.5.1",
    "wrangler": "^2.1.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "3.1.1",
    "typescript": "4.4.3",
    "worktop.build": "0.0.3"
  }
}
