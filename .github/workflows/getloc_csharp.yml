name: Get LoC - C#

on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *'

jobs:
  getloc:
    uses: PabloLec/StarryLines/.github/workflows/run.yml@main
    with:
      args: 'getloc csharp'
    secrets: inherit